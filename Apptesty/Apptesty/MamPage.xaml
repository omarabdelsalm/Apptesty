<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:zxing="clr-namespace:ZXing.Net.Mobile.Forms;assembly=ZXing.Net.Mobile.Forms" xmlns:zxingcommon="clr-namespace:ZXing.Common;assembly=zxing"
             x:Class="Apptesty.MamPage">
    <ContentPage.Content>
        <StackLayout>
            <SearchBar TextChanged="SearchBar_TextChanged"/>
            <ListView x:Name="listvw" IsPullToRefreshEnabled="True" HasUnevenRows="true" SeparatorVisibility="Default" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>

                            <Grid Padding="10" BackgroundColor="DimGray" Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <zxing:ZXingBarcodeImageView Grid.RowSpan="1" BarcodeFormat="QR_CODE"  WidthRequest="50" HeightRequest="50" BarcodeValue="$'Entry{txtId.Text}'" >
                                        <zxing:ZXingBarcodeImageView.BarcodeOptions>
                                            <zxingcommon:EncodingOptions Height="10" Width="10" Margin="10"/>
                                        </zxing:ZXingBarcodeImageView.BarcodeOptions>
                                    </zxing:ZXingBarcodeImageView>
                                
                                <Label Grid.RowSpan="1"
                                           Text="{Binding PhNum}" TextColor="Wheat"/>
                                <Label Grid.Column="1"
                                                Text="{Binding Name}"
                                                      FontAttributes="Bold" TextColor="Blue"/>
                                <Label Grid.Row="1"
                                             
                                        Text="{Binding PointNum}" TextColor="Red"/>
                                <Label Grid.Row="1"
                                            
                                        Text="{Binding txtId}"  TextColor="Red"/>

                            </Grid>
                        </ViewCell>

                        <!--<TextCell Text="{Binding Name}"></TextCell>
                            
                                <Label Text="{Binding PhNum}"></Label>
                            
                            <TextCell Text="{Binding PointNum}"></TextCell>-->

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>